{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC (events, window drag) + app commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
