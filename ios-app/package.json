{
  "name": "clawed-ios",
  "version": "0.1.0",
  "private": true,
  "description": "Claw-ED iOS thin client — a calm Capacitor wrapper that connects to a teacher's own Claw-ED server over the LAN. No Python runs on the device.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "add:ios": "cap add ios",
    "sync": "cap sync ios",
    "sync:ios": "cap sync ios",
    "open": "cap open ios",
    "open:ios": "cap open ios",
    "copy": "cap copy ios",
    "doctor": "cap doctor"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
